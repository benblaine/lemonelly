# reference/ — internal working material (committed, never deployed)

Excluded from Vercel via `.vercelignore`, so nothing in here is reachable on
lemonelly.com. Safe for prospect research and design references.

- `inspiration/` — screenshots and template references Ben supplies; the draft
  template's design system derives from these.
- `clients/<slug>.md` — one fact sheet per prospect: extracted facts with source
  URLs and fetch dates, brand notes, and a status line. Facts only — no sales
  commentary. This doubles as Ben's outreach pipeline (`ls reference/clients/`).
